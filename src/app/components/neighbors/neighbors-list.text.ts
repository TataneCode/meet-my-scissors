export const text: NeighborsListTextModel = {
    loading: 'Loading...',
    error: 'Something went wrong...',
    message: 'No neighbor found !',
};

interface NeighborsListTextModel {
    loading: string;
    error: string;
    message: string;
}